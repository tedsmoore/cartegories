services:
  db:
    image: postgres:16
    restart: always
    ports:
      - "2345:5432"
    volumes:
      - ./.db:/var/lib/postgresql/data
      - ./.db-dumps/:/dumps
    environment:
      POSTGRES_USER: cartegories
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cartegories
